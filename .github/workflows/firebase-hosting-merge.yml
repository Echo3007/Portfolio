# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

# name: Deploy to Firebase Hosting on merge
# on:
#   push:
#     branches:
#       - main
# jobs:
#   build_and_deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - run: npm install && next build
#       - uses: FirebaseExtended/action-hosting-deploy@v0
#         with:
#           repoToken: ${{ secrets.GITHUB_TOKEN }}
#           firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_PORTFOLIO_ANGELA }}
#           channelId: live
#           projectId: portfolio-angela


name: Deploy to Firebase Hosting on merge # This is your MERGE workflow
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js environment
        uses: actions/setup-node@v4
        with:
          node-version: '20.11.1'

      # CORRECT THIS LINE FOR THE MERGE WORKFLOW
      - name: Install dependencies and build
        run: npm install && npm run build
        working-directory: ./portfolio/ # <--- ADDED/CORRECTED THIS!

      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_PORTFOLIO_ANGELA }}
          channelId: live
          projectId: portfolio-angela
          # If Next.js output is 'out' (for static export), you might also need:
          # entryPoint: ./portfolio/out # Or where your build output goes, relative to repo root