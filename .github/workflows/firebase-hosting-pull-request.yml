# # This file was auto-generated by the Firebase CLI
# # https://github.com/firebase/firebase-tools

# name: Deploy to Firebase Hosting on PR
# on: pull_request
# permissions:
#   checks: write
#   contents: read
#   pull-requests: write
# jobs:
#   build_and_preview:
#     if: ${{ github.event.pull_request.head.repo.full_name == github.repository }}
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - run: npm install && next build
#       - uses: FirebaseExtended/action-hosting-deploy@v0
#         with:
#           repoToken: ${{ secrets.GITHUB_TOKEN }}
#           firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_PORTFOLIO_ANGELA }}
#           projectId: portfolio-angela
# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR # This is your PR workflow
on: pull_request
permissions:
  checks: write
  contents: read
  pull-requests: write
jobs:
  build_and_preview:
    if: ${{ github.event.pull_request.head.repo.full_name == github.repository }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # ADD THIS NODE.JS SETUP FOR THE PR WORKFLOW
      - name: Set up Node.js environment
        uses: actions/setup-node@v4
        with:
          node-version: '20.11.1' # Ensure this matches your desired Node.js version

      # CORRECT THIS LINE FOR THE PR WORKFLOW
      - run: npm install && npm run build # Changed 'next build' to 'npm run build'

      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_PORTFOLIO_ANGELA }}
          projectId: portfolio-angela

--- # This is a YAML document separator, good for clarity if you keep them in one file.

name: Deploy to Firebase Hosting on merge # This is your MERGE workflow
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Set up Node.js (this one is already correct)
      - name: Set up Node.js environment
        uses: actions/setup-node@v4
        with:
          node-version: '20.11.1' # Or your desired Node.js version, e.g., '18', '22'. Check your local Node.js version with `node -v`.

      - run: npm install && npm run build # This one is already correct
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_PORTFOLIO_ANGELA }}
          channelId: live
          projectId: portfolio-angela