# # This file was auto-generated by the Firebase CLI
# # https://github.com/firebase/firebase-tools

# name: Deploy to Firebase Hosting on PR
# on: pull_request
# permissions:
#   checks: write
#   contents: read
#   pull-requests: write
# jobs:
#   build_and_preview:
#     if: ${{ github.event.pull_request.head.repo.full_name == github.repository }}
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - run: npm install && next build
#       - uses: FirebaseExtended/action-hosting-deploy@v0
#         with:
#           repoToken: ${{ secrets.GITHUB_TOKEN }}
#           firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_PORTFOLIO_ANGELA }}
#           projectId: portfolio-angela


# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
on: pull_request
permissions:
  checks: write
  contents: read
  pull-requests: write
jobs:
  build_and_preview:
    if: ${{ github.event.pull_request.head.repo.full_name == github.repository }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js environment
        uses: actions/setup-node@v4
        with:
          # Try using a broader Node 20 or even 22 if compatible
          node-version: '20' # Use '20' for latest Node 20, or '22' for Node 22
          # cache: 'npm' # Consider adding this for faster future builds once successful

      # --- START OF CHANGES FOR NATIVE MODULES ---
      - name: Install dependencies (including native modules)
        run: |
          npm cache clean --force # Ensure a clean cache
          npm ci --loglevel verbose # Use verbose for more output during install
        working-directory: ./portfolio/

      - name: Attempt to reinstall sharp specifically (if previous step fails)
        # This step only runs if the previous one fails or if you want to be extra cautious.
        # This will install sharp specific for linux-x64 and might help relink.
        # Note: 'npm install' might modify package-lock.json here, which is usually avoided in CI
        # but is sometimes necessary for stubborn native modules.
        # Only uncomment and use this if the above npm ci fails again.
        # run: npm install --os=linux --cpu=x64 sharp
        # working-directory: ./portfolio/

      - name: Build Project
        run: npm run build
        working-directory: ./portfolio/
      # --- END OF CHANGES FOR NATIVE MODULES ---

      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_PORTFOLIO_ANGELA }}
          projectId: portfolio-angela
